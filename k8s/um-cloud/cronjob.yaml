#T* Deployment
apiVersion: batch/v1
kind: CronJob
metadata:
  name: cronjob
  namespace: martingreyes-tif
spec:
  schedule: "35 21 * * *" # Ejecuta todos los días a las 21:35 (UTC)
  timeZone: "America/Argentina/Buenos_Aires"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: cronjob
            image: martingreyes/tif-cronjob:latest
            command: ["python", "app.py"]
            envFrom:
              - configMapRef:
                  name: cronjob-config
              - secretRef:
                  name: cronjob-secret
          restartPolicy: OnFailure
---
#T* ConfigMap
apiVersion: v1
kind: ConfigMap
metadata:
  name: cronjob-config
  namespace: martingreyes-tif
data:
  ENDPOINT_HOST: webscraping-service
  ENDPOINT_PORT: "9080"
  DB_HOST: mariadb-service
  DB_PORT: "3306"
  DB_NAME: Versus
  REDIS_HOST: redis-service
  REDIS_PORT: "6379"



